---
# 服务器配置

# 服务器IP；默认 0.0.0.0
Host              : 0.0.0.0

# 服务器端口；此为内部运行端口，请将此端口映射到 nginx 上
Port              : 3000

# 站点名称
site_name         : &site_name 管理平台

# 站点链接
site_url          : http://localhost:3000

# 设置 Session 名称
session_secret    : nuxt-web-admin

# 设置 Redis 缓存库
redis:
  host            : 127.0.0.1
  port            : 6379
  db              : 0

# 设置 MongoDB 数据库
mongodb:
  uris            : mongodb://localhost:27017/nuxt-web-admin
  options:
    useNewUrlParser        : true
    useCreateIndex         : true
    useUnifiedTopology     : true

# 默认语言
language          : zh-cn

# 自定义选项
options:
  # 上传文件默认目录
  store_root        : uploadfiles
  # 绑定 Request 头部信息
  headers:
    # - !!seq [ Access-Control-Allow-Origin, '*' ]
    # - !!seq [ Access-Control-Allow-Methods, 'GET, HEAD, POST, PUT, PATCH, DELETE' ]
    - !!seq [ Access-Control-Allow-Headers, 'X-Requested-With,content-type, Authorization' ]
  # 错误信息
  errorInfo: |-
    {
      "error": {{ code }},
      "message": "{{ message }}"
    }
  # 页面标题
  title             : *site_name
  # Meta 信息
  meta:
    - 
      charset           : utf-8
    - 
      name              : viewport
      content           : width=device-width, initial-scale=1
    - 
      hid               : description
      name              : description
      content           : 基于 Nuxt.js 与 Express 构建而成。
